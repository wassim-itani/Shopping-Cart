const Counter = () => {
  return (
    <span className="absolute -top-1 -right-1 flex justify-center items-center w-4 h-4 bg-black text-xs text-white rounded-full">
      0
    </span>
  );
};

export default Counter;
